apiVersion: apps/v1
kind: Deployment
metadata:
  name: registration-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: registration-service
  template:
    metadata:
      labels:
        app: registration-service
    spec:
      containers:
        - name: registration-service
          image: docker.io/library/registration-service:latest
          imagePullPolicy: Never
          envFrom:
            - secretRef:
                name: db-credentials
          ports:
            - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: registration-service
spec:
  selector:
    app: registration-service
  ports:
    - port: 3000
      targetPort: 3000
