apiVersion: apps/v1
kind: Deployment
metadata:
  name: event-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: event-service
  template:
    metadata:
      labels:
        app: event-service
    spec:
      containers:
        - name: event-service
          image: docker.io/library/event-service:latest
          imagePullPolicy: Never
          envFrom:
            - secretRef:
                name: db-credentials
          ports:
            - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: event-service
spec:
  selector:
    app: event-service
  ports:
    - port: 3000
      targetPort: 3000
